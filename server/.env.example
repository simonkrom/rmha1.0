JWT_SECRET=change_this_secret
PORT=4000
DB_PATH=./data/backend.db
 # Postgres connection URL, e.g. postgres://user:pass@localhost:5432/laboratoire
 DATABASE_URL=postgres://lab_user:lab_pass@db:5432/laboratoire

# Optional Supabase settings (for using Supabase instead of local Postgres)
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=service_role_key_here   # Server only (DO NOT expose in the browser)
# SUPABASE_ANON_KEY=anon_key_here                    # Optional: for direct frontend access (with RLS)
